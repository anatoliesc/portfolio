Function:
this .bas file is to be used in conjunction with a blank Excel workbook.

It pulls the Attendance statistics from ESPN.NBA's website. It pulls basic statistics
of game attendance for their teams and imports it into an Excel workbook with
appropriately named sheets.

In order to use do the following:
Create a new workbook, enable Macros, and import the .bas file from the VBA Editor
within Excel. After that, run the Macros in the following order: